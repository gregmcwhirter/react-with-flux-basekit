$ = jQuery = require('jquery');
var React = require('react');
var ReactDOM = require('react-dom');
var Base = require('./components/dashboard');

ReactDOM.render(<Base />, document.getElementById('app'));

